# RAKSHA AI — lightweight Render runtime
# Hosted Groq inference replaces local ML/CV/RAG stacks.
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets>=13.0
pydantic==2.11.4
pydantic-settings==2.9.1
python-multipart==0.0.20
cryptography>=42.0.0

# Application integrations
supabase==2.15.0
httpx==0.28.1
groq>=0.12.0
# Small codec used only to convert WEBP/BMP uploads to Groq-compatible JPEG.
Pillow>=10.4.0

# Runs the API and optional lightweight WhatsApp bridge sidecar.
supervisor>=4.2.0
